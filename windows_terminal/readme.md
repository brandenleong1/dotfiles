# Windows Terminal

For the fields `"schemes"` and `"themes"` in `settings.json`, copy the values into the corresponding fields in `settings.json` found through the bottom left "Open JSON File" button in the Windows Terminal settings menu.
